# arp-spoof-script
A script that place you in between the router and target computer!

1. Enable port forwarding on the linux machine by - echo 1 > /proc/sys/net/ipv4/ip_forward
2. Then run the script for it to work!
